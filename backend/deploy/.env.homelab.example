# Copy to deploy/.env.homelab and fill in. NEVER commit the real file.
# Supabase SESSION pooler (IPv4, supports migration advisory locks):
DATABASE_URL=postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres
JWT_SECRET=replace-with-a-32-plus-char-random-secret
CORS_ALLOWED_ORIGINS=https://<your-app>.vercel.app
# Optional: stable named tunnel (recommended). Get from Cloudflare Zero Trust >
# Networks > Tunnels > create tunnel > copy token; map a hostname to localhost:8080.
# CLOUDFLARED_TUNNEL_TOKEN=

# ── Magic-link email (SMTP). Without these, magic-link is disabled. ──
# Public app URL — magic-link emails point here (e.g. https://web-alpha-three-89.vercel.app).
APP_URL=https://web-alpha-three-89.vercel.app
# Gmail SMTP: create an App Password at Google Account > Security > App passwords.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@gmail.com
SMTP_PASS=your-16-char-app-password
# Optional friendly From (defaults to SMTP_USER):
# SMTP_FROM=SeaSteps <you@gmail.com>
