# SeaSteps frontend — zmienne środowiskowe
#
# Adres backendu (Rust/Axum API).
#   Puste            = tryb MOCK (dane demo, bez backendu)
#   http://localhost:8080  = lokalny backend (docker-compose / scripts/dev-up.sh)
#   https://...      = prod
#
# Skopiuj do .env.local (gitignorowane) i ustaw wg potrzeb.
VITE_API_BASE=http://localhost:8080
